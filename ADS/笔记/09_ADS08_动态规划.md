# ADS08 动态规划（dynamic programming） —— 课前笔记

> 来源：`../Materials/Student Slides/ADS08DynamicProgramming_Stu.ppt`（*Dynamic Programming*）
> 教材参考（讲义末页给出）：CLRS 3rd Ch.15 p.359–413；讲义同时引用 Weiss 教材的图号（见各例）
> 整理日期：2026-09-16
> 说明：本文由 AI 依据讲义幻灯片整理，用于课前预习；表格数值与逐步填表动画请看课件。

## 1. 一页速览

| 例子 | 递归（recursion）/穷举的代价 | 动态规划的代价 | 讲义要点 |
|---|---|---|---|
| 1. Fibonacci | $T(N)\ge F(N)$（指数级） | $T(N)=O(N)$ | **用表格代替递归**：子问题只算一次 |
| 2. 矩阵连乘次序 | 方案数是 Catalan 数（Catalan number） $b_n=\Omega(4^n/n^{1.5})$ | $O(N^3)$ | 递推（recurrence） + 按"区间长度 $k$"递增填表（table filling） |
| 3. 最优二叉搜索树 | 枚举树形也是指数 | $O(N^3)$（讲义指出 $O(N^2)$ 的改进版，见教材 10.33，p.419） | 静态查找（static search）的最优树 |
| 4. 全源最短路 | 对每个源跑单源算法 $O(|V|^3)$ | $O(N^3)$ | Floyd-Warshall：一维一维地"加入中间顶点（intermediate vertex）" |
| 5. 流水线装配 | 穷举 $O(2^N)$ 时间 + $O(N)$ 空间 | $O(N)$ 时间 + $O(N)$ 空间 | 讲义用来演示**DP 四步设计法** |

一句话：**动态规划 = 记录子问题答案的表格 + 按某种顺序填表**；它的两个前提是**最优子结构**与**子问题重叠（overlapping subproblems）**。

## 2. 五个经典例子

### 2.1 Fibonacci：先看清"重复计算"有多贵

```c
int Fib( int N ) { if ( N <= 1 ) return 1; else return Fib( N-1 ) + Fib( N-2 ); }
```

【讲义】递归树（recursion tree）显示 $F_6$ 里 $F_2$、$F_1$、$F_0$ 被反复计算（trouble-maker：**冗余计算的增长是爆炸式的**）；改进版只保存最近两个值，$T(N)=O(N)$：

```c
int Fibonacci ( int N ) {  /* F(0)=F(1)=1 */
    int i, Last, NextToLast, Answer;
    if ( N <= 1 ) return 1;
    Last = NextToLast = 1;
    for ( i = 2; i <= N; i++ ) { Answer = Last + NextToLast; NextToLast = Last; Last = Answer; }
    return Answer;
}
```

### 2.2 矩阵连乘次序（Ordering Matrix Multiplications）

- 【讲义】例子：$M_1[10\times20]\cdot M_2[20\times50]\cdot M_3[50\times1]\cdot M_4[1\times100]$
  - 按 $M_1(M_2(M_3M_4))$ 算：$50\cdot1\cdot100+20\cdot50\cdot100+10\cdot20\cdot100=125{,}000$；
  - 按 $(M_1(M_2M_3))M_4$ 算：$20\cdot50\cdot1+10\cdot20\cdot1+10\cdot1\cdot100=2{,}200$。
- 方案数 $b_n=\sum b_i b_{n-i}$ 是 **Catalan 数**（$b_2=1,b_3=2,b_4=5,\dots$），穷举不可行。
- 定义 $m_{ij}$ = 计算 $M_i\cdots M_j$ 的最优代价（$M_i$ 是 $r_{i-1}\times r_i$）：

$$m_{ij}=\min_{i\le L<j}\big\{\,m_{iL}+m_{L+1,j}+r_{i-1}r_Lr_j\,\big\} \tag{1}$$

- 关键观察【讲义】：$M_{ij}$ 只有 $O(N^2)$ 个；若 $j-i=k$，计算它只需要 $j-i<k$ 的值 ⇒ **按 $k=j-i$ 从小到大填表**。
- 讲义给出 `OptMatrix( const long r[ ], int N, TwoDimArray M )` 的实现（三层循环 `k / i / L`），$T(N)=O(N^3)$；要记录加括号（parenthesization）顺序见教材 **Figure 10.46, p.388**。

### 2.3 最优二叉搜索树（Optimal Binary Search Tree）

- 场景：$N$ 个词 $w_1<w_2<\dots<w_N$（**静态查找，不插入不删除**），查找（search）概率（probability） $p_i$，要排列成期望（expected value）访问时间最小的 BST。
- 【讲义】例子给出的概率表：

| word | break | case | return | switch | char | do | void |
|---|---|---|---|---|---|---|---|
| probability | 0.22 | 0.18 | 0.25 | 0.02 | 0.20 | 0.05 | 0.08 |

- 记号：$T_{ij}$ = $w_i..w_j$ 的 OBST，$c_{ij}$ 代价、$r_{ij}$ 根、$w_{ij}$ 权重（$=\sum_{k=i}^{j}p_k$），$c_{ii}=0$。
- 递推（根取 $k$ 时，左右两棵子树的所有节点深度都 +1，故要把权重再加一遍）：

$$c_{ij}=p_k+c_{i,k-1}+c_{k+1,j}+w_{i,k-1}+w_{k+1,j}
\quad\Longrightarrow\quad
c_{ij}=w_{ij}+c_{i,k-1}+c_{k+1,j} \tag{2}$$

  $T_{ij}$ 最优 ⇔ 取使式 (2) 最小的 $k$ 作 $r_{ij}$。
- 【讲义】的填表结果中，$c_{1N}=2.15$（即 $w_1..w_N$ 整体最优代价），对应树形见课件。
- 复杂度 $T(N)=O(N^3)$；讲义提示教材 **10.33, p.419** 有 $O(N^2)$ 的改进（可用"四边形不等式"性质，见拓展阅读）。

### 2.4 全源最短路（All-Pairs Shortest Path）

- 方法 1：对每个顶点（vertex）跑一次单源算法，$T=O(|V|^3)$（在**稀疏图**上更快）。
- 方法 2【讲义】：定义

$$D^{k}[i][j]=\min\{\ \text{从 } i \text{ 到 } j,\ \text{中间点只取 }\{l\le k\}\ \text{的路径长度}\},\qquad D^{-1}[i][j]=\text{Cost}[i][j]$$

  逐步生成 $D^0,D^1,\dots,D^{N-1}$，答案 $D^{N-1}[i][j]$。要点是"顶点 $k$ 用不用"：

$$D^{k}[i][j]=\min\big(D^{k-1}[i][j],\ D^{k-1}[i][k]+D^{k-1}[k][j]\big) \tag{3}$$

- 讲义给出 `AllPairs( A, D, N )` 的三重循环实现（就地更新 $D$ 即可），$T(N)=O(N^3)$，在**稠密图（dense graph）**上更快；
- **允许负权边（negative-weight edge），但不能有负权环（negative-weight cycle）**；判据：若 $D[i][i]<0$ 则存在负权环；
- 记录路径（path）见教材 **Figure 10.53, p.393**。

### 2.5 流水线装配（Product Assembly）——DP 的设计方法论

两条装配线、每站时间不同、站间可以换线，目标是最小化总装配时间。

- 【讲义】穷举：$O(2^N)$ 时间 + $O(N)$ 空间。
- **四步设计法**（本页是全讲的"方法论总结"，建议背下来）：

| 步骤 | 本页内容 |
|---|---|
| 1. 刻画最优解（optimal solution）的结构 | *An optimal solution contains an optimal solution of a sub-problem!* |
| 2. 递归定义最优值 | `f[line][stage] = min( f[line][stage-1]+t_process[line][stage-1], f[1-line][stage-1]+t_transit[1-line][stage-1] )` |
| 3. 按顺序计算 | `for stage=1..n { for line=0..1 {…} }`，$O(N)$ 时间 + $O(N)$ 空间 |
| 4. 重构解（方案） | 用 `L[line][stage]` 记录"来自哪条线"，再从 $n$ 倒推回去得到 `plan[]` |

- 【讲义】结论（**Elements of DP**）：**最优子结构（optimal substructure）** + **重叠子问题（overlapping sub-problems）**。
  课堂讨论 *Discussion 11*：*When can't we apply dynamic programming?*（预习时想一想：子问题不重叠、或不满足最优子结构时就不适用——例如依赖"历史路径是否访问过某点"的最短路变体。）

## 3. 听课重点与易混点

1. **DP 的本质不是"递推"，而是"避免重复计算子问题"**：先要有重叠子问题，填表才有意义。
2. **填表顺序由依赖关系决定**：矩阵连乘按"区间长度 $k$ 递增"，Floyd 按"中间顶点 $k$ 递增"。
3. **OBST 的递推里为什么要再加一次权重（weight） $w_{ij}$**：根一旦确定，左右子树（right subtree）的每个节点（node）深度（depth）都 +1，期望代价整体增加它们的概率之和。
4. **Floyd 的正确性依赖"逐步放开中间顶点"**，不能写成"先选一个 $k$ 就定下来"。
5. **重构解（solution reconstruction）需要额外的表**（`L[][]`、根的记录 $r_{ij}$、路径记录），只算最优值不够。

## 4. 讲义中的例子（课上会看到）

- $F_6$ 的递归树（哪些子问题被重复计算）；
- 4 矩阵的两种加括号方式与代价对比；
- OBST 的完整填表（从长度 1 的区间一路填到 $c_{1,7}=2.15$）；
- Floyd 的 `AllPairs` 代码与负环判据；
- 装配线（assembly line）的 $f$、$L$ 两张表与最终 `plan[]` 的重构代码。

## 5. 课前自测

1. 写出 Fibonacci 的递归与迭代（iteration）版本，并说明重复计算的规模。
2. 矩阵连乘为什么不能穷举？写出 $m_{ij}$ 的递推式（recurrence）与填表顺序。
3. 写出 OBST 的递推式，并解释 $w_{ij}$ 这一项的来源。
4. 写出 Floyd 的 $D^k[i][j]$ 定义与转移方程；如何检测负权环？
5. DP 的四个设计步骤分别是什么？"最优子结构"和"重叠子问题"各指什么？
6. 举一个"看起来很 DP 但不能用 DP"的场景（对应 *Discussion 11*）。

## 6. 来源与延伸阅读

- 讲义：`ADS08DynamicProgramming_Stu.ppt`（含讲稿备注：*decision problem is easier* 等对 NP 部分的前置铺垫）。
- 教材：CLRS Ch.15（p.359–413）；Weiss 的 Figure 10.46 (p.388)、10.53 (p.393)、10.33 (p.419)。
- 拓展阅读：`../Materials/Further Reading/Efficient Dynamic Programming Using Quadrangle Inequalities.pdf`（与 OBST 的 $O(N^2)$ 改进相关）。
- 本讲配套的研究项目：*Research Project 3 — Beautiful Subsequence*。
- 待确认：讲义中 OBST 的填表数值、流水线（assembly line）的例子参数在幻灯片中以图形呈现，本文只引用其结论（$c_{1N}=2.15$、$O(N)$ 时间与空间）。
